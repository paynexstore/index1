# Tournament Master BD 

Built with [WebAPK Builder Pro](https://github.com)

## Build

This project uses GitHub Actions to automatically build the APK.

Push to main branch → Actions tab → Download APK artifact.

## Manual Build

```bash
./gradlew assembleDebug
```

APK: `app/build/outputs/apk/debug/app-debug.apk`
